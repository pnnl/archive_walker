'Imports System.Windows.Controls

Public Class ScalarRepCustomization
    'Private Sub ComboBox_SelectionChanged(sender As Object, e As Windows.Controls.SelectionChangedEventArgs)
    '    If TypeOf sender Is ComboBox Then
    '        Dim cb As ComboBox = DirectCast(sender, ComboBox)
    '        Dim selectedValue = cb.SelectedValue
    '        Dim selectedItem = cb.SelectedItem
    '        Dim selectedIndex = cb.SelectedIndex
    '    End If
    'End Sub

    'Private Sub ComboBox_SelectionChanged_1(sender As Object, e As Windows.Controls.SelectionChangedEventArgs)
    '    If TypeOf sender Is ComboBox Then
    '        Dim cb As ComboBox = DirectCast(sender, ComboBox)
    '        Dim selectedValue = cb.SelectedValue
    '        Dim selectedItem = cb.SelectedItem
    '        Dim selectedIndex = cb.SelectedIndex
    '    End If
    'End Sub
End Class
